Hash Function
=============

**Function**:

.. function:: Hash(data []byte) ([]byte)

    Compute a SHA-512 hash of the message.

    :param data: Arbitrary-length data to hash
    :type data: []byte

    :returns: 64-byte hash of the message
    :rtype: []byte
